[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "toolkit"
version = "1.0.0"
description = "Console calculator and unit converter"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]